apiVersion: v1
kind: ConfigMap
metadata:
  name: postgres-init
  namespace: redactd
data:
  init.sql: |
    CREATE DATABASE platform_db;
    CREATE DATABASE content_db;
    CREATE DATABASE verdict_db;
